{
  "name": "blackroad-api",
  "version": "1.0.0",
  "description": "BlackRoad backend API — Stripe, LLM, portals",
  "main": "server_full.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "node server_full.js",
    "test": "node --test 'tests/**/*.test.js'",
    "test:e2e": "node --test 'tests/e2e/*.test.js'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^14.14.0"
  }
}
